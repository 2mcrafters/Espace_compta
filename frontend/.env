# Frontend environment variables (Vite)
# Backend base URL (Laravel)
# In dev we serve Laravel with PHP on 127.0.0.1:8002 (see VS Code tasks)
VITE_BACKEND_URL=http://localhost:8000

# Frontend origin (this Vite dev server)
VITE_FRONTEND_URL=http://localhost:5173
# API base path (proxied)
VITE_API_BASE=/api
